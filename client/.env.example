# API base URL. In development, use the API's HTTPS profile so the Secure,
# SameSite=None refresh cookie is sent from the SPA to the API.
# Trust the dev certificate first:  dotnet dev-certs https --trust
VITE_API_BASE_URL=http://localhost:5244
